{
  "name": "incidents",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": {
    "type": "git",
    "url": "git@github.wdf.sap.corp:FE-SAMPLES/fe-garage-demo.git"
  },
  "license": "SAP SAMPLE CODE LICENSE",
  "dependencies": {
    "@sap/cds": "^3",
    "@sap/hana-client": "^2",
    "@sap/xssec": "^2",
    "express": "^4",
    "hdb": "^0.17.2"
  },
  "engines": {
    "node": "^12"
  },
  "scripts": {
    "build": "cds build/all --clean",
    "deploy": "cds deploy",
    "start": "cds watch"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sql"
      },
      "[development]": {
        "requires": {
          "db": {
            "kind": "sqlite",
            "credentials": {
              "database": ":memory:"
            }
          }
        }
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ],
  "devDependencies": {
    "@sap/ux-specification": "^1.78.6",
    "sqlite3": "^4.2.0"
  },
  "sapux": [
    "app/incidents"
  ]
}
